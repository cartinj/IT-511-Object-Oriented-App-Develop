public class VirtualWorld { // where we set our main elements for the final project
public static void main(String[] args)
{
virtual.world.MyClone clone; // creates a new clone
    clone = new virtual.world.MyClone();
clone.setFirstName("#myname"); // sets the clone's first name
clone.setLastName("#mylname"); // sets the clone's last name
clone.introduction(); // invokes introduction
}
}
